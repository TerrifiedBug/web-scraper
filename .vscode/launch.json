{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run src/",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/stock_scraper.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/src",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}